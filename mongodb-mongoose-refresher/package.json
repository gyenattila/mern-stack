{
  "name": "mongodb-mongoose-refresher",
  "version": "1.0.0",
  "description": "A NoSQL Database which stores `Documents` in `Collections` (instead of `Records` in `Tables` as in SQL).",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "mongodb": "^4.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
